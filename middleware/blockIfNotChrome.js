export default function ({ from, store, redirect, route }) {
    // if(window.location.host === "neo.foyr.com")
    // if (
    //     window &&
    //     window.localStorage &&
    //     window.localStorage.skipChromeCheck
    // ) {
    //     return;
    // }
    // if((store.$ua.deviceType() == 'pc' && store.$ua.browser() != 'Chrome') ){
    //     return redirect('/blocked')
    // }
  }